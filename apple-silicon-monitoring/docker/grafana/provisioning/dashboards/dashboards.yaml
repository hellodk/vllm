apiVersion: 1

providers:
  - name: 'Apple Silicon LLM'
    orgId: 1
    folder: 'Apple Silicon LLM'
    folderUid: 'apple-silicon-llm'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
